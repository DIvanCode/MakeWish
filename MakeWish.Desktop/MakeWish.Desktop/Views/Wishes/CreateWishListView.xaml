<UserControl x:Class="MakeWish.Desktop.Views.Wishes.CreateWishListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:wishes="clr-namespace:MakeWish.Desktop.ViewModels.Wishes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=wishes:CreateWishListViewModel}">
    <materialDesign:Card Padding="32" Margin="16, 0, 16, 16" Background="LightCyan">
        <StackPanel>
            <TextBlock Text="Новый список желаний" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,32"/>

            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Название"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     Width="400"
                     Margin="0,0,0,16"/>

            <Button Content="Создать"
                    Command="{Binding CreateCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Height="35"
                    Width="400"/>
        </StackPanel>
    </materialDesign:Card>
</UserControl>
