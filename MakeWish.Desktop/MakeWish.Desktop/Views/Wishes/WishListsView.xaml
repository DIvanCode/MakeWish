<UserControl x:Class="MakeWish.Desktop.Views.Wishes.WishListsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:wishes="clr-namespace:MakeWish.Desktop.ViewModels.Wishes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=wishes:WishListsViewModel}">
    <materialDesign:Card Padding="32" Margin="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
                <Button Content="Желания"
                        Command="{Binding ShowMainWishListCommand}"
                        Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="Списки желаний"
                        Command="{Binding ShowWishListsCommand}"
                        Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignFlatButton}">
                            <Setter Property="BorderThickness" Value="0,0,0,2"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource PrimaryHueMidBrush}"/>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <ItemsControl Grid.Column="0"
                              ItemsSource="{Binding WishLists}"
                              Width="300"
                              HorizontalAlignment="Left">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                                <Button Style="{StaticResource MaterialDesignFlatButton}"
                                        Margin="0,0,0,10"
                                        Width="300" >
                                    <StackPanel Orientation="Horizontal">
                                        <materialDesign:PackIcon Kind="PlaylistPlay"
                                                                 Width="24"
                                                                 Height="24"
                                                                 Margin="0,0,8,0"
                                                                 VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Title}"
                                                   Margin="4,0,4,0"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </Button>
                                <StackPanel Orientation="Horizontal">
                                    <ItemsControl ItemsSource="{Binding Wishes}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <StackPanel Orientation="Horizontal"  Height="100" >
                                                    <Button Style="{StaticResource MaterialDesignFlatButton}"
                                                            Command="{Binding DataContext.SelectWishCommand,
                                                                RelativeSource={RelativeSource AncestorType=Grid}}"
                                                            CommandParameter="{Binding Id}"
                                                            Margin="0,0,0,10"
                                                            Height="100" >
                                                        <Image Height="100" Source="{Binding ImageUrl}"/>
                                                    </Button>
                                                </StackPanel>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>    
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <ContentControl Grid.Column="1" Content="{Binding SelectedWishViewModel}"/>
            </Grid>
        </Grid>
    </materialDesign:Card>
</UserControl> 